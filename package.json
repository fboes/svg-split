{
  "name": "svg-split",
  "version": "1.0.3",
  "description": "Split a single SVG file into multiple SVG files, each file containing one single SVG element.",
  "main": "bin/svg-split.js",
  "bin": {
    "svg-split": "bin/svg-split.js"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "eslint": "npx eslint bin src",
    "eslint-fix": "npx eslint --fix bin src",
    "prettier": "npx prettier --cache --check .",
    "prettier-fix": "npx prettier --cache --write .",
    "test": "npm run eslint && npm run prettier"
  },
  "keywords": [
    "svg",
    "cli"
  ],
  "type": "module",
  "author": "Frank Boës <info@3960.org> (https://3960.org/)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fboes/svg-split.git"
  },
  "bugs": {
    "url": "https://github.com/fboes/svg-split/issues"
  },
  "homepage": "https://github.com/fboes/svg-split#readme",
  "devDependencies": {
    "eslint": "^8.41.0",
    "prettier": "2.8.8"
  }
}
